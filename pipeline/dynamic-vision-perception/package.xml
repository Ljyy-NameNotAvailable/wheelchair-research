<?xml version="1.0"?>
<?xml-model href="http://download.ros.org/schema/package_format3.xsd" schematypens="http://www.w3.org/2001/XMLSchema"?>
<package format="3">
  <name>dynamic_perception</name>
  <version>0.1.0</version>
  <description>
    Dynamic components sub-system for the vision-only indoor wheelchair perception pipeline.
    Implements a 3-layer architecture: dynamic mask generation (YOLO + RAFT-S optical flow),
    SLAM protection (ORB-SLAM3 mask injection), and extended scene state publishing.
    Spec: research/vision-only-perception-pipeline/dynamic-components-design.md
  </description>
  <maintainer email="research@wheelchair-project.local">Wheelchair Research</maintainer>
  <license>MIT</license>

  <!-- Build dependencies -->
  <buildtool_depend>ament_cmake</buildtool_depend>
  <buildtool_depend>ament_cmake_python</buildtool_depend>

  <!-- ROS2 runtime dependencies -->
  <depend>rclpy</depend>
  <depend>sensor_msgs</depend>
  <depend>std_msgs</depend>
  <depend>geometry_msgs</depend>
  <depend>cv_bridge</depend>

  <!-- Python package dependencies (also in requirements.txt) -->
  <!-- ultralytics, torch, torchvision, opencv-python, pyyaml, numpy -->

  <export>
    <build_type>ament_cmake</build_type>
  </export>
</package>
